# MongoDB Configuration 
spring.data.mongodb.uri=mongodb://localhost:27017/flightDB
spring.data.mongodb.auto-index-creation=true

# Ignite Configuration
ignite.workDirectory=./ignite/work
ignite.persistenceEnabled=false
ignite.clientMode=false

# Repository configuration (remove spring.data.mongodb.repositories.type=auto)